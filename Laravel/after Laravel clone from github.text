1. 進到專案資料夾
    $cd project

2. 安裝composer套件.npm套件
    $composer install
    $npm install

3. 重新生成ssh-key
    $php artisan key:generate

touch database/database.sqlite
mysql> CREATE DATABASE your_database_name;

php artisan migrate
php artisan db:seed
php artisan migrate --seed
php artisan storage:link
php artisan config:clear